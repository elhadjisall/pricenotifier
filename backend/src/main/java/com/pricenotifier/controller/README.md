# Controller Package

This package contains all REST controllers for the application.
Controllers handle HTTP requests and responses, and delegate business logic to the service layer.

Example structure:
- ProductController
- PriceController
- NotificationController 